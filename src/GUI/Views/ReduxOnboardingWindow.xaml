<adonisUI:AdonisWindow xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:adonisUI="clr-namespace:AdonisUI.Controls;assembly=AdonisUI" xmlns:controls="clr-namespace:DivinityModManager.Controls" xmlns:dapp="clr-namespace:DivinityModManager;assembly=DivinityModManagerCore" xmlns:data="clr-namespace:DivinityModManager.Models;assembly=DivinityModManagerCore" xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework" x:Class="DivinityModManager.Views.ReduxOnboardingWindow" Title="Welcome Setup" Width="740" Height="720" MinWidth="640" MinHeight="480" MaxWidth="900" MaxHeight="780" SizeToContent="Manual" ResizeMode="CanResize" ShowInTaskbar="False" WindowStartupLocation="CenterOwner" WindowStyle="SingleBorderWindow" Template="{DynamicResource ReduxWindowTemplate}" Background="{DynamicResource ReduxWindowBrush}" FontFamily="{DynamicResource Redux.FontFamily.UI}" FontSize="{DynamicResource Redux.FontSize.12}" FontWeight="{DynamicResource Redux.FontWeight.Body}" TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="ClearType">
	<shell:WindowChrome.WindowChrome>
		<shell:WindowChrome CaptionHeight="0" CornerRadius="0" GlassFrameThickness="0" ResizeBorderThickness="6" UseAeroCaptionButtons="False"/>
	</shell:WindowChrome.WindowChrome>
	<Window.Resources>
		<ResourceDictionary>
			<ResourceDictionary.MergedDictionaries>
				<ResourceDictionary Source="../Themes/MainResourceDictionary.xaml"/>
			</ResourceDictionary.MergedDictionaries>
			<Style x:Key="WelcomeDetailsStyle" TargetType="Expander">
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="Expander">
							<StackPanel>
								<ToggleButton FocusVisualStyle="{StaticResource ReduxManagerKeyboardFocusStyle}" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}" Content="{TemplateBinding Header}" Foreground="{DynamicResource ReduxTextSecondaryBrush}" HorizontalContentAlignment="Left" Cursor="Hand">
									<ToggleButton.Template>
										<ControlTemplate TargetType="ToggleButton">
											<Border x:Name="Surface" Background="Transparent" Padding="4,7" CornerRadius="4">
												<StackPanel Orientation="Horizontal">
													<controls:ReduxIcon x:Name="Arrow" StrokeData="{DynamicResource Redux.Icon.ChevronRightStroke}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Width="14" Height="14" Margin="0,0,7,0" VerticalAlignment="Center" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
													<ContentPresenter VerticalAlignment="Center"/>
												</StackPanel>
											</Border>
											<ControlTemplate.Triggers>
												<Trigger Property="IsChecked" Value="True">
													<Setter TargetName="Arrow" Property="StrokeData" Value="{DynamicResource Redux.Icon.ChevronDownStroke}"/>
												</Trigger>
												<Trigger Property="IsMouseOver" Value="True">
													<Setter TargetName="Surface" Property="Background" Value="{DynamicResource ReduxHoverBrush}"/>
												</Trigger>
											</ControlTemplate.Triggers>
										</ControlTemplate>
									</ToggleButton.Template>
								</ToggleButton>
								<Border x:Name="Details" Height="0" ClipToBounds="True" Visibility="Collapsed">
									<ContentPresenter/>
								</Border>
							</StackPanel>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
			<Style x:Key="WelcomeSectionStyle" TargetType="Border">
				<Setter Property="Padding" Value="0"/>
				<Setter Property="Background" Value="Transparent"/>
				<Setter Property="BorderBrush" Value="{DynamicResource ReduxBorderBrush}"/>
				<Setter Property="BorderThickness" Value="0"/>
				<Setter Property="CornerRadius" Value="{DynamicResource Redux.CornerRadius.Card}"/>
			</Style>
			<Style x:Key="WelcomeSectionTitleStyle" TargetType="TextBlock">
				<Setter Property="FontSize" Value="{DynamicResource Redux.FontSize.14}"/>
				<Setter Property="FontWeight" Value="{DynamicResource Redux.FontWeight.Title}"/>
				<Setter Property="Foreground" Value="{DynamicResource ReduxTextPrimaryBrush}"/>
			</Style>
			<Style x:Key="WelcomeChoiceTitleStyle" TargetType="TextBlock">
				<Setter Property="FontWeight" Value="{DynamicResource Redux.FontWeight.Control}"/>
				<Setter Property="Foreground" Value="{DynamicResource ReduxTextPrimaryBrush}"/>
				<Setter Property="TextWrapping" Value="Wrap"/>
			</Style>
			<Style x:Key="WelcomeChoiceDescriptionStyle" TargetType="TextBlock">
				<Setter Property="Margin" Value="0,3,0,0"/>
				<Setter Property="FontSize" Value="{DynamicResource Redux.FontSize.10}"/>
				<Setter Property="Foreground" Value="{DynamicResource ReduxTextMutedBrush}"/>
				<Setter Property="TextWrapping" Value="Wrap"/>
			</Style>
			<Style x:Key="WelcomeFeatureCardStyle" TargetType="Border">
				<Setter Property="Padding" Value="10"/>
				<Setter Property="Background" Value="{DynamicResource ReduxSurfaceElevatedBrush}"/>
				<Setter Property="BorderBrush" Value="{DynamicResource ReduxBorderBrush}"/>
				<Setter Property="BorderThickness" Value="1"/>
				<Setter Property="CornerRadius" Value="{DynamicResource Redux.CornerRadius.Control}"/>
			</Style>
			<Style x:Key="WelcomeThemeCardStyle" TargetType="RadioButton">
				<Setter Property="Margin" Value="0,0,8,0"/>
				<Setter Property="Cursor" Value="Hand"/>
				<Setter Property="FocusVisualStyle" Value="{x:Null}"/>
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="RadioButton">
							<Border x:Name="Card" Padding="10" Background="{DynamicResource ReduxSurfaceElevatedBrush}" BorderBrush="{DynamicResource ReduxBorderBrush}" BorderThickness="1" CornerRadius="{DynamicResource Redux.CornerRadius.Control}">
								<ContentPresenter/>
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="IsMouseOver" Value="True">
									<Setter TargetName="Card" Property="Background" Value="{DynamicResource ReduxHoverBrush}"/>
									<Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource ReduxAccentHoverBrush}"/>
								</Trigger>
								<Trigger Property="IsChecked" Value="True">
									<Setter TargetName="Card" Property="Background" Value="{DynamicResource ReduxAccentSoftBrush}"/>
									<Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource ReduxAccentBrush}"/>
									<Setter TargetName="Card" Property="BorderThickness" Value="2"/>
								</Trigger>
								<Trigger Property="IsKeyboardFocused" Value="True">
									<Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource ReduxAccentBrush}"/>
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
			<Style x:Key="WelcomeStepStyle" TargetType="Button">
				<Setter Property="Foreground" Value="{DynamicResource ReduxTextMutedBrush}"/>
				<Setter Property="Background" Value="Transparent"/>
				<Setter Property="BorderBrush" Value="{DynamicResource ReduxBorderBrush}"/>
				<Setter Property="BorderThickness" Value="0,0,0,2"/>
				<Setter Property="Padding" Value="0,10"/>
				<Setter Property="Cursor" Value="Hand"/>
				<Setter Property="HorizontalContentAlignment" Value="Left"/>
				<Setter Property="FocusVisualStyle" Value="{StaticResource ReduxManagerKeyboardFocusStyle}"/>
				<Setter Property="Template">
					<Setter.Value>
						<ControlTemplate TargetType="Button">
							<Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
								<ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center"/>
							</Border>
							<ControlTemplate.Triggers>
								<Trigger Property="IsMouseOver" Value="True">
									<Setter Property="Foreground" Value="{DynamicResource ReduxTextPrimaryBrush}"/>
								</Trigger>
							</ControlTemplate.Triggers>
						</ControlTemplate>
					</Setter.Value>
				</Setter>
			</Style>
			<Style x:Key="WelcomeCurrentStepStyle" TargetType="Button" BasedOn="{StaticResource WelcomeStepStyle}">
				<Setter Property="Foreground" Value="{DynamicResource ReduxTextPrimaryBrush}"/>
				<Setter Property="BorderBrush" Value="{DynamicResource ReduxAccentBrush}"/>
				<Setter Property="FontWeight" Value="SemiBold"/>
			</Style>
			<Style x:Key="WelcomeCaptionStyle" TargetType="TextBlock">
				<Setter Property="Foreground" Value="{DynamicResource ReduxTextMutedBrush}"/>
				<Setter Property="TextWrapping" Value="Wrap"/>
				<Setter Property="Margin" Value="0,6,0,0"/>
			</Style>
		</ResourceDictionary>
	</Window.Resources>
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
		</Grid.RowDefinitions>
		<controls:ReduxWindowTitleBar IconData="{DynamicResource Redux.Icon.ReduxStar}" ShowWindowControls="False"/>
		<Border Grid.Row="1" Padding="28,8,28,20" Background="{DynamicResource ReduxAppBackgroundBrush}">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="*"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>
				<StackPanel Margin="0,0,0,24">
					<Grid Margin="0,0,0,24">
						<Grid.ColumnDefinitions>
							<ColumnDefinition/>
							<ColumnDefinition/>
							<ColumnDefinition/>
							<ColumnDefinition/>
						</Grid.ColumnDefinitions>
						<Button x:Name="SetupStepButton" Grid.Column="0" Tag="0" Click="StepButton_Click" Margin="0,0,12,0" Style="{StaticResource WelcomeStepStyle}" Content="01   Make it yours"/>
						<Button x:Name="SourcesStepButton" Grid.Column="1" Tag="1" Click="StepButton_Click" Margin="0,0,12,0" Style="{StaticResource WelcomeStepStyle}" Content="02   Add mods"/>
						<Button x:Name="OrderStepButton" Grid.Column="2" Tag="2" Click="StepButton_Click" Margin="0,0,12,0" Style="{StaticResource WelcomeStepStyle}" Content="03   Load order"/>
						<Button x:Name="ManagersStepButton" Grid.Column="3" Tag="3" Click="StepButton_Click" Margin="0,0,0,0" Style="{StaticResource WelcomeStepStyle}" Content="04   More to explore"/>
					</Grid>
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>
						<StackPanel>
							<TextBlock x:Name="StepProgressText" Visibility="Collapsed"/>
							<TextBlock x:Name="StepTitleText" FontSize="28" FontWeight="SemiBold" TextWrapping="Wrap" Foreground="{DynamicResource ReduxTextPrimaryBrush}"/>
							<TextBlock x:Name="StepDescriptionText" Margin="0,8,0,0" TextWrapping="Wrap" Foreground="{DynamicResource ReduxTextSecondaryBrush}"/>
						</StackPanel>
						<controls:ReduxIcon Width="42" Height="42" StrokeData="{DynamicResource Redux.Icon.ReduxStar}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Grid.Column="1" Margin="20,0,4,0" Opacity="0.7" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
					</Grid>
				</StackPanel>
				<ScrollViewer Grid.Row="1" x:Name="OnboardingContentScrollViewer" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
					<Grid>
						<StackPanel x:Name="AppearancePage">
							<Border Style="{StaticResource WelcomeFeatureCardStyle}" Padding="14" Margin="0,0,0,22">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
										<ColumnDefinition Width="Auto"/>
									</Grid.ColumnDefinitions>
									<controls:ReduxIcon Width="26" Height="26" StrokeData="{DynamicResource Redux.Icon.FolderOpen}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Margin="0,0,14,0" VerticalAlignment="Center" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
									<StackPanel Grid.Column="1">
										<TextBlock Text="Baldur’s Gate 3" TextWrapping="Wrap" FontWeight="SemiBold"/>
										<TextBlock Text="" x:Name="GameStatusText" Style="{StaticResource WelcomeCaptionStyle}"/>
										<TextBlock x:Name="GamePathText" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource ReduxTextMutedBrush}" Margin="0,3,8,0"/>
									</StackPanel>
									<Button Click="ChooseGame_Click" Style="{StaticResource ReduxSecondaryActionButtonStyle}" Grid.Column="2" VerticalAlignment="Center">
										<StackPanel Orientation="Horizontal">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.FolderOpen}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Text="Browse…" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
											</Grid>
										</StackPanel>
									</Button>
								</Grid>
							</Border>
							<TextBlock Text="Choose your look" Style="{StaticResource WelcomeSectionTitleStyle}" TextWrapping="Wrap"/>
							<UniformGrid Columns="3" Margin="0,12,0,0">
								<RadioButton x:Name="ReduxDarkThemeCard" GroupName="WelcomeThemes" Style="{StaticResource WelcomeThemeCardStyle}" Tag="{x:Static data:ReduxThemeType.ReduxDark}" Click="ThemeCard_Click" AutomationProperties.Name="Dark theme">
									<StackPanel>
										<Border Height="100" Background="#17121D" CornerRadius="5" Padding="10">
											<Grid>
												<Grid.RowDefinitions>
													<RowDefinition Height="12"/>
													<RowDefinition Height="*"/>
												</Grid.RowDefinitions>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="30"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<Border Grid.ColumnSpan="2" Background="#33283F" CornerRadius="2" Margin="0,0,0,6"/>
												<Border Grid.Row="1" Background="#33283F" CornerRadius="3" Margin="0,3,7,0"/>
												<StackPanel Grid.Row="1" Grid.Column="1" Margin="0,3,0,0">
													<Border Background="#9676FF" Height="10" CornerRadius="3"/>
													<Border Background="#33283F" Height="6" CornerRadius="2" Margin="0,9,0,0"/>
													<Border Background="#33283F" Height="6" CornerRadius="2" Margin="0,7,18,0"/>
													<Border Background="#33283F" Height="6" CornerRadius="2" Margin="0,7,8,0"/>
												</StackPanel>
											</Grid>
										</Border>
										<TextBlock Foreground="{DynamicResource ReduxTextPrimaryBrush}" Text="Dark" Margin="0,10,0,0" FontWeight="SemiBold"/>
									</StackPanel>
								</RadioButton>
								<RadioButton x:Name="ReduxLightThemeCard" GroupName="WelcomeThemes" Style="{StaticResource WelcomeThemeCardStyle}" Tag="{x:Static data:ReduxThemeType.ReduxLight}" Click="ThemeCard_Click" AutomationProperties.Name="Light theme">
									<StackPanel>
										<Border Height="100" Background="#F1EDF4" CornerRadius="5" Padding="10">
											<Grid>
												<Grid.RowDefinitions>
													<RowDefinition Height="12"/>
													<RowDefinition Height="*"/>
												</Grid.RowDefinitions>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="30"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<Border Grid.ColumnSpan="2" Background="#DDD6E5" CornerRadius="2" Margin="0,0,0,6"/>
												<Border Grid.Row="1" Background="#DDD6E5" CornerRadius="3" Margin="0,3,7,0"/>
												<StackPanel Grid.Row="1" Grid.Column="1" Margin="0,3,0,0">
													<Border Background="#694AD6" Height="10" CornerRadius="3"/>
													<Border Background="#DDD6E5" Height="6" CornerRadius="2" Margin="0,9,0,0"/>
													<Border Background="#DDD6E5" Height="6" CornerRadius="2" Margin="0,7,18,0"/>
													<Border Background="#DDD6E5" Height="6" CornerRadius="2" Margin="0,7,8,0"/>
												</StackPanel>
											</Grid>
										</Border>
										<TextBlock Foreground="{DynamicResource ReduxTextPrimaryBrush}" Text="Light" Margin="0,10,0,0" FontWeight="SemiBold"/>
									</StackPanel>
								</RadioButton>
								<RadioButton x:Name="ParchmentThemeCard" GroupName="WelcomeThemes" Style="{StaticResource WelcomeThemeCardStyle}" Tag="{x:Static data:ReduxThemeType.Parchment}" Click="ThemeCard_Click" AutomationProperties.Name="Parchment theme">
									<StackPanel>
										<Border Height="100" Background="#EBDEC5" CornerRadius="5" Padding="10">
											<Grid>
												<Grid.RowDefinitions>
													<RowDefinition Height="12"/>
													<RowDefinition Height="*"/>
												</Grid.RowDefinitions>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="30"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<Border Grid.ColumnSpan="2" Background="#D1C0A2" CornerRadius="2" Margin="0,0,0,6"/>
												<Border Grid.Row="1" Background="#D1C0A2" CornerRadius="3" Margin="0,3,7,0"/>
												<StackPanel Grid.Row="1" Grid.Column="1" Margin="0,3,0,0">
													<Border Background="#8B3034" Height="10" CornerRadius="3"/>
													<Border Background="#D1C0A2" Height="6" CornerRadius="2" Margin="0,9,0,0"/>
													<Border Background="#D1C0A2" Height="6" CornerRadius="2" Margin="0,7,18,0"/>
													<Border Background="#D1C0A2" Height="6" CornerRadius="2" Margin="0,7,8,0"/>
												</StackPanel>
											</Grid>
										</Border>
										<TextBlock Foreground="{DynamicResource ReduxTextPrimaryBrush}" Text="Parchment" Margin="0,10,0,0" FontWeight="SemiBold"/>
									</StackPanel>
								</RadioButton>
							</UniformGrid>
							<TextBlock Text="Create your own themes later in Preferences." Style="{StaticResource WelcomeCaptionStyle}"/>
							<UniformGrid Columns="2" Margin="0,16,0,0">
								<CheckBox x:Name="ReduceMotionCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Checked="AccessibilityCheckBox_Changed" Unchecked="AccessibilityCheckBox_Changed" Margin="0,4,16,4" VerticalContentAlignment="Center">
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="Auto"/>
											<ColumnDefinition Width="*"/>
										</Grid.ColumnDefinitions>
										<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Flash}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
										<TextBlock Text="Reduce motion" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
									</Grid>
								</CheckBox>
								<CheckBox x:Name="DisableBackgroundEffectsCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Checked="AccessibilityCheckBox_Changed" Unchecked="AccessibilityCheckBox_Changed" Margin="0,4,16,4" VerticalContentAlignment="Center">
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="Auto"/>
											<ColumnDefinition Width="*"/>
										</Grid.ColumnDefinitions>
										<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Eye}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
										<TextBlock Text="Disable blur and dimming" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
									</Grid>
								</CheckBox>
							</UniformGrid>
							<Expander x:Name="AppearanceOptionsExpander" Header="Appearance options" Style="{StaticResource WelcomeDetailsStyle}" Expanded="BeforePlayExpansion_Changed" Collapsed="BeforePlayExpansion_Changed" Margin="0,8,0,0">
								<StackPanel Margin="4,8,4,0">
									<UniformGrid Columns="2">
										<CheckBox x:Name="HideIconsCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Click="AppearanceOption_Click" Margin="0,6,16,6" VerticalContentAlignment="Center">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.ListStroke}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Grid.Column="1" Text="Hide icons" TextWrapping="Wrap" VerticalAlignment="Center"/>
											</Grid>
										</CheckBox>
										<CheckBox x:Name="IconsOnlyCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Click="AppearanceOption_Click" Margin="0,6,16,6" VerticalContentAlignment="Center">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Albums}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Grid.Column="1" Text="Show icons without labels" TextWrapping="Wrap" VerticalAlignment="Center"/>
											</Grid>
										</CheckBox>
										<CheckBox x:Name="CategorySelectionCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Click="AppearanceOption_Click" Margin="0,6,16,6" VerticalContentAlignment="Center">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.ReorderStroke}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Grid.Column="1" Text="Use category colors for selection" TextWrapping="Wrap" VerticalAlignment="Center"/>
											</Grid>
										</CheckBox>
										<CheckBox x:Name="CategoryTextCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Click="AppearanceOption_Click" Margin="0,6,16,6" VerticalContentAlignment="Center">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Create}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Grid.Column="1" Text="Use category colors for text" TextWrapping="Wrap" VerticalAlignment="Center"/>
											</Grid>
										</CheckBox>
										<CheckBox x:Name="GradientsCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Click="AppearanceOption_Click" Margin="0,6,16,6" VerticalContentAlignment="Center">
											<Grid>
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Sparkles}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Grid.Column="1" Text="Use generated gradients" TextWrapping="Wrap" VerticalAlignment="Center"/>
											</Grid>
										</CheckBox>
									</UniformGrid>
									<Grid Margin="0,12,0,0">
										<Grid.ColumnDefinitions>
											<ColumnDefinition/>
											<ColumnDefinition Width="16"/>
											<ColumnDefinition/>
										</Grid.ColumnDefinitions>
										<StackPanel Grid.Column="0">
											<TextBlock Foreground="{DynamicResource ReduxTextSecondaryBrush}" Text="Font" Margin="0,0,0,5"/>
											<ComboBox x:Name="WelcomeFontComboBox" AutomationProperties.Name="Font" MinHeight="32" SelectionChanged="AppearanceSelection_Changed"/>
										</StackPanel>
										<StackPanel Grid.Column="2">
											<TextBlock Foreground="{DynamicResource ReduxTextSecondaryBrush}" Text="Text size" Margin="0,0,0,5"/>
											<ComboBox x:Name="WelcomeTextSizeComboBox" AutomationProperties.Name="Text size" MinHeight="32" SelectionChanged="AppearanceSelection_Changed"/>
										</StackPanel>
									</Grid>
								</StackPanel>
							</Expander>
						</StackPanel>
						<StackPanel x:Name="ConnectionsPage" Visibility="Collapsed">
							<Grid Margin="0,0,0,22">
								<Grid.ColumnDefinitions>
									<ColumnDefinition/>
									<ColumnDefinition Width="16"/>
									<ColumnDefinition/>
								</Grid.ColumnDefinitions>
								<Border Grid.Column="0" Style="{StaticResource WelcomeFeatureCardStyle}" Padding="18">
									<StackPanel>
										<controls:ReduxIcon Width="32" Height="32" StrokeData="{DynamicResource Redux.Icon.Archive}" Foreground="{DynamicResource ReduxAccentHoverBrush}" HorizontalAlignment="Left" Margin="0,0,0,14" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
										<TextBlock Text="Drop a file" Style="{StaticResource WelcomeSectionTitleStyle}" TextWrapping="Wrap"/>
										<TextBlock Text="Drag a PAK or archive into Redux, or choose Add Package." Style="{StaticResource WelcomeCaptionStyle}"/>
									</StackPanel>
								</Border>
								<Border Grid.Column="2" Style="{StaticResource WelcomeFeatureCardStyle}" Padding="18">
									<StackPanel>
										<Viewbox Width="32" Height="32" HorizontalAlignment="Left" Margin="0,0,0,14" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}">
											<ContentControl Width="16" Height="16" ContentTemplate="{DynamicResource NexusPlatformIconTemplate}"/>
										</Viewbox>
										<TextBlock Text="Download from Nexus" Style="{StaticResource WelcomeSectionTitleStyle}" TextWrapping="Wrap"/>
										<TextBlock Text="Choose Mod Manager Download on Nexus Mods to send it to Redux." Style="{StaticResource WelcomeCaptionStyle}"/>
									</StackPanel>
								</Border>
							</Grid>
							<CheckBox x:Name="SourceIntegrationsCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Checked="SourceIntegrationsCheckBox_Changed" Unchecked="SourceIntegrationsCheckBox_Changed">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>
									<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.LinkStroke}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
									<TextBlock Text="Connect mod services" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
								</Grid>
							</CheckBox>
							<TextBlock Text="Get mod details and update information from Nexus Mods and mod.io." Style="{StaticResource WelcomeCaptionStyle}"/>
							<Border x:Name="SourceCredentialsPanel" Visibility="Collapsed" Margin="0,16,0,0" Padding="16" Background="{DynamicResource ReduxSurfaceBrush}" CornerRadius="8">
								<StackPanel>
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition/>
											<ColumnDefinition Width="16"/>
											<ColumnDefinition/>
										</Grid.ColumnDefinitions>
										<StackPanel Grid.Column="0">
											<Grid Margin="0,0,0,7">
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<ContentControl Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" ContentTemplate="{DynamicResource NexusPlatformIconTemplate}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Text="Nexus Mods API key" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
											</Grid>
											<PasswordBox x:Name="NexusApiKeyTextBox" AutomationProperties.Name="Nexus Mods API key" MinHeight="34"/>
										</StackPanel>
										<StackPanel Grid.Column="2">
											<Grid Margin="0,0,0,7">
												<Grid.ColumnDefinitions>
													<ColumnDefinition Width="Auto"/>
													<ColumnDefinition Width="*"/>
												</Grid.ColumnDefinitions>
												<ContentControl Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" ContentTemplate="{DynamicResource ModioPlatformIconTemplate}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock Text="mod.io API key (optional)" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
											</Grid>
											<PasswordBox x:Name="ModioApiKeyTextBox" AutomationProperties.Name="mod.io API key (optional)" MinHeight="34"/>
										</StackPanel>
									</Grid>
									<TextBlock Text="Keys stay on this computer. Change them in Preferences." Style="{StaticResource WelcomeCaptionStyle}"/>
									<CheckBox x:Name="NxmLinksCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Margin="0,16,0,0">
										<Grid>
											<Grid.ColumnDefinitions>
												<ColumnDefinition Width="Auto"/>
												<ColumnDefinition Width="*"/>
											</Grid.ColumnDefinitions>
											<ContentControl Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" ContentTemplate="{DynamicResource NexusPlatformIconTemplate}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
											<TextBlock Text="Open Nexus download links in Redux" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
										</Grid>
									</CheckBox>
									<TextBlock Text="A Nexus API key is required for downloads." Style="{StaticResource WelcomeCaptionStyle}"/>
								</StackPanel>
							</Border>
						</StackPanel>
						<StackPanel x:Name="OptionsPage" Visibility="Collapsed">
							<Border Background="{DynamicResource ReduxSurfaceBrush}" CornerRadius="10" BorderBrush="{DynamicResource ReduxBorderBrush}" BorderThickness="1">
								<StackPanel>
									<Grid Margin="18,14">
										<Grid.ColumnDefinitions>
											<ColumnDefinition/>
											<ColumnDefinition Width="Auto"/>
										</Grid.ColumnDefinitions>
										<TextBlock Text="Your load order" FontWeight="SemiBold"/>
										<TextBlock Grid.Column="1" Text="PRACTICE" Foreground="{DynamicResource ReduxTextMutedBrush}"/>
									</Grid>
									<Border Background="{DynamicResource ReduxBorderBrush}" Height="1"/>
									<Grid Margin="18,12,18,14">
										<Grid.ColumnDefinitions>
											<ColumnDefinition/>
											<ColumnDefinition Width="32"/>
											<ColumnDefinition/>
										</Grid.ColumnDefinitions>
										<StackPanel>
											<TextBlock Text="Inactive" Foreground="{DynamicResource ReduxTextMutedBrush}" Margin="0,0,0,12"/>
											<Border Style="{StaticResource WelcomeFeatureCardStyle}" MinHeight="70">
												<StackPanel>
													<controls:ReduxIcon Width="22" Height="22" StrokeData="{DynamicResource Redux.Icon.Archive}" Foreground="{DynamicResource ReduxAccentHoverBrush}" HorizontalAlignment="Left" Margin="0,0,0,10" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
													<TextBlock x:Name="DemoInactiveText" Text="Example mod" TextWrapping="Wrap"/>
												</StackPanel>
											</Border>
										</StackPanel>
										<controls:ReduxIcon Width="18" Height="18" StrokeData="{DynamicResource Redux.Icon.ChevronRightStroke}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Grid.Column="1" VerticalAlignment="Center" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
										<StackPanel Grid.Column="2">
											<TextBlock Text="Active" Foreground="{DynamicResource ReduxTextMutedBrush}" Margin="0,0,0,12"/>
											<Border Style="{StaticResource WelcomeFeatureCardStyle}" MinHeight="70">
												<StackPanel>
													<controls:ReduxIcon Width="22" Height="22" StrokeData="{DynamicResource Redux.Icon.ReorderStroke}" Foreground="{DynamicResource ReduxAccentHoverBrush}" HorizontalAlignment="Left" Margin="0,0,0,10" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
													<TextBlock x:Name="DemoActiveText" Text="No active mods" TextWrapping="Wrap"/>
												</StackPanel>
											</Border>
										</StackPanel>
									</Grid>
									<Border Background="{DynamicResource ReduxSurfaceElevatedBrush}" Padding="18,14" CornerRadius="0,0,10,10">
										<StackPanel>
											<TextBlock x:Name="DemoProgressText" Text="01 / Activate" Foreground="{DynamicResource ReduxAccentHoverBrush}" FontWeight="SemiBold"/>
											<TextBlock x:Name="DemoInstructionText" Text="Installed mods start here. Move one to Active to use it." TextWrapping="Wrap" Margin="0,6,0,10"/>
											<Button x:Name="DemoActionButton" Click="DemoAction_Click" HorizontalAlignment="Left" Style="{StaticResource ReduxPrimaryActionButtonStyle}">
												<Grid>
													<Grid.ColumnDefinitions>
														<ColumnDefinition Width="Auto"/>
														<ColumnDefinition Width="*"/>
													</Grid.ColumnDefinitions>
													<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.ChevronRightStroke}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType={x:Type Button}}}" x:Name="DemoActionIcon" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
													<TextBlock x:Name="DemoActionLabel" Text="Activate mod" Grid.Column="1" VerticalAlignment="Center"/>
												</Grid>
											</Button>
										</StackPanel>
									</Border>
								</StackPanel>
							</Border>
							<CheckBox x:Name="GuidanceCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Checked="GuidanceCheckBox_Changed" Unchecked="GuidanceCheckBox_Changed" Margin="0,18,0,0" ToolTip="Offers suggestions for active mods. You review changes before applying them.">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>
									<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Flag}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
									<TextBlock Text="Enable Load Order Advisor (experimental)" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
								</Grid>
							</CheckBox>
							<CheckBox x:Name="RetainArchivesCheckBox" Style="{StaticResource ReduxCheckBoxStyle}" Margin="0,10,0,0" ToolTip="Uses additional disk space. Off by default.">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>
									<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Archive}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
									<TextBlock Text="Keep installed archives for reinstalling" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
								</Grid>
							</CheckBox>
							<Expander x:Name="BeforePlayExpander" Header="Before you play" Style="{StaticResource WelcomeDetailsStyle}" Expanded="BeforePlayExpansion_Changed" Collapsed="BeforePlayExpansion_Changed" Margin="0,8,0,0">
								<TextBlock Margin="4,8" Text="Install each mod’s requirements, check Mod Diagnostics, then sync your order. Keep the same mod setup when returning to an existing save." TextWrapping="Wrap" Foreground="{DynamicResource ReduxTextSecondaryBrush}"/>
							</Expander>
						</StackPanel>
						<StackPanel x:Name="ManagersPage" Visibility="Collapsed">
							<Grid Margin="0,0,0,12">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="*"/>
									<ColumnDefinition Width="12"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>
								<Button x:Name="SaveTourButton" Tag="save" Click="ManagerTourSelection_Click" Style="{StaticResource WelcomeCurrentStepStyle}">
									<StackPanel Orientation="Horizontal">
										<controls:ReduxIcon Width="20" Height="20" Margin="0,0,8,0" StrokeData="{DynamicResource Redux.Icon.Save}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
										<TextBlock Text="Save games" VerticalAlignment="Center"/>
									</StackPanel>
								</Button>
								<Button x:Name="NativeTourButton" Grid.Column="2" Tag="native" Click="ManagerTourSelection_Click" Style="{StaticResource WelcomeStepStyle}">
									<StackPanel Orientation="Horizontal">
										<controls:ReduxIcon Width="20" Height="20" Margin="0,0,8,0" StrokeData="{DynamicResource Redux.Icon.Shield}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
										<TextBlock Text="Native mods" VerticalAlignment="Center"/>
									</StackPanel>
								</Button>
							</Grid>
							<Border Background="{DynamicResource ReduxSurfaceBrush}" CornerRadius="10" Padding="18">
								<StackPanel>
									<TextBlock Text="Import a save · Preview" x:Name="TourTitle" Margin="0,0,0,14" Style="{StaticResource WelcomeSectionTitleStyle}"/>
									<Grid>
										<Grid.ColumnDefinitions>
											<ColumnDefinition Width="*"/>
											<ColumnDefinition Width="40"/>
											<ColumnDefinition Width="*"/>
										</Grid.ColumnDefinitions>
										<Border Style="{StaticResource WelcomeFeatureCardStyle}">
											<StackPanel>
												<controls:ReduxIcon x:Name="TourFileIcon" Width="32" Height="32" HorizontalAlignment="Left" Margin="0,0,0,10" Foreground="{DynamicResource ReduxAccentHoverBrush}" StrokeData="{DynamicResource Redux.Icon.Save}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock x:Name="TourFileName" Text="Example save.zip" TextWrapping="Wrap" FontWeight="SemiBold"/>
												<TextBlock x:Name="TourFileDescription" Text="A save file, folder, or ZIP" TextWrapping="Wrap" Margin="0,4,0,0" Foreground="{DynamicResource ReduxTextSecondaryBrush}"/>
											</StackPanel>
										</Border>
										<controls:ReduxIcon Grid.Column="1" Width="22" Height="22" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.ChevronRightStroke}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
										<Border Grid.Column="2" Style="{StaticResource WelcomeFeatureCardStyle}">
											<StackPanel>
												<controls:ReduxIcon x:Name="TourDestinationIcon" Width="32" Height="32" HorizontalAlignment="Left" Margin="0,0,0,10" Foreground="{DynamicResource ReduxAccentHoverBrush}" StrokeData="{DynamicResource Redux.Icon.FolderOpen}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
												<TextBlock x:Name="TourDestination" Text="Save Game Manager" FontWeight="SemiBold" TextWrapping="Wrap"/>
												<TextBlock x:Name="TourResult" Text="Ready for your save" Margin="0,4,0,0" TextWrapping="Wrap" Foreground="{DynamicResource ReduxTextSecondaryBrush}" AutomationProperties.LiveSetting="Polite"/>
											</StackPanel>
										</Border>
									</Grid>
									<TextBlock x:Name="TourExplanation" Text="Drop a save file, folder, or ZIP into Redux." Margin="0,14,0,12" TextWrapping="Wrap"/>
									<Button ToolTip="Practice only. No files are changed." x:Name="TourActionButton" Click="ManagerTourAction_Click" HorizontalAlignment="Left" Style="{StaticResource ReduxSecondaryActionButtonStyle}">
										<StackPanel Orientation="Horizontal">
											<controls:ReduxIcon Width="14" Height="14" Margin="0,0,6,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Download}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
											<TextBlock x:Name="TourActionLabel" Text="Preview import"/>
										</StackPanel>
									</Button>
								</StackPanel>
							</Border>
							<Border Style="{StaticResource WelcomeFeatureCardStyle}" Margin="0,12,0,0">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>
									<controls:ReduxIcon Width="20" Height="20" Margin="0,0,10,0" VerticalAlignment="Top" StrokeData="{DynamicResource Redux.Icon.FolderOpen}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
									<StackPanel Grid.Column="1">
										<TextBlock x:Name="TourLocation" Text="Saves → Manage" FontWeight="SemiBold" TextWrapping="Wrap"/>
										<TextBlock x:Name="TourTip" Text="Browse, import, and open the selected profile’s saves." Margin="0,5,0,0" TextWrapping="Wrap" Foreground="{DynamicResource ReduxTextSecondaryBrush}"/>
									</StackPanel>
								</Grid>
							</Border>
							<CheckBox x:Name="OpenDownloadsCheckBox" IsChecked="True" Margin="0,12,0,0" Style="{StaticResource ReduxCheckBoxStyle}">
								<Grid>
									<Grid.ColumnDefinitions>
										<ColumnDefinition Width="Auto"/>
										<ColumnDefinition Width="*"/>
									</Grid.ColumnDefinitions>
									<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Download}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
									<TextBlock Text="Open Download Manager when I finish" TextWrapping="Wrap" Grid.Column="1" VerticalAlignment="Center"/>
								</Grid>
							</CheckBox>
						</StackPanel>
					</Grid>
				</ScrollViewer>
				<Grid Grid.Row="2" Margin="0,20,0,0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>
					<Button x:Name="NotNowButton" Click="CloseButton_Click" HorizontalAlignment="Left" IsCancel="True" Style="{StaticResource ReduxSecondaryActionButtonStyle}">
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto"/>
								<ColumnDefinition Width="*"/>
							</Grid.ColumnDefinitions>
							<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.Close}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
							<TextBlock Text="Set up later" Grid.Column="1" VerticalAlignment="Center"/>
						</Grid>
					</Button>
					<StackPanel Grid.Column="1" Orientation="Horizontal">
						<Button x:Name="BackButton" Click="BackButton_Click" Visibility="Collapsed" Margin="0,0,8,0" Style="{StaticResource ReduxSecondaryActionButtonStyle}">
							<Grid>
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto"/>
									<ColumnDefinition Width="*"/>
								</Grid.ColumnDefinitions>
								<controls:ReduxIcon Width="16" Height="16" Margin="0,0,8,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.ChevronLeftStroke}" Style="{StaticResource ReduxOptionalInterfaceIconStyle}" Foreground="{DynamicResource ReduxAccentHoverBrush}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
								<TextBlock Text="Back" Grid.Column="1" VerticalAlignment="Center"/>
							</Grid>
						</Button>
						<Button x:Name="SaveContinueButton" Click="SaveButton_Click" IsDefault="True" MinWidth="110" Style="{StaticResource ReduxPrimaryActionButtonStyle}">
							<StackPanel Orientation="Horizontal">
								<TextBlock x:Name="ContinueLabel" Text="Continue"/>
								<controls:ReduxIcon x:Name="ContinueIcon" Width="14" Height="14" Margin="8,0,0,0" VerticalAlignment="Center" StrokeData="{DynamicResource Redux.Icon.ChevronRightStroke}" Visibility="{Binding Path=(dapp:DivinityApp.ShowInterfaceIcons), Converter={StaticResource BoolToVisibilityConverter}}"/>
							</StackPanel>
						</Button>
					</StackPanel>
				</Grid>
			</Grid>
		</Border>
	</Grid>
</adonisUI:AdonisWindow>
